git clone --filter=blob:none --no-checkout https://github.com/sdparekh/zUMIs.git
cd zUMIs

git sparse-checkout init --no-cone

git sparse-checkout set zUMIs "*.sh" "*.pl" "*.R" "*.py" "*.pm"

git checkout`



git init
    git remote add origin https://github.com/sdparekh/zUMIs.git
    git config core.sparseCheckout true

    echo "zUMIs" >> .git/info/sparse-checkout
    echo "*.sh" >> .git/info/sparse-checkout
    echo "*.pl" >> .git/info/sparse-checkout
    echo "*.R" >> .git/info/sparse-checkout
    echo "*.py" >> .git/info/sparse-checkout
    echo "*.pm" >> .git/info/sparse-checkout
    git pull origin main

    mkdir -p $PREFIX/share/zumis
    cp -r * $PREFIX/share/zumis/
